# lambda-local-runner
Super simple cli tool for running NodeJS lambda functions locally as a server.

Does not attempt to run any kind of gateway or load balancing layer.

Uses `jiti` for universal compatibility.
